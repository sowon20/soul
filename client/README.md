# Soul Client - Frontend

Soul AI Assistant의 프론트엔드 UI

## 🚀 빠른 시작

```bash
# 패키지 설치
npm install

# 개발 서버 실행 (http://localhost:3000)
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 미리보기
npm run preview
```

## 📁 프로젝트 구조

```
client/
├── index.html              # 메인 HTML
├── package.json            # 의존성
├── vite.config.js          # Vite 설정
└── src/
    ├── main.js             # 앱 진입점
    ├── styles/             # CSS 파일들
    │   ├── main.css        # 메인 스타일
    │   ├── themes.css      # 테마 시스템 (6가지 스킨)
    │   ├── components.css  # 컴포넌트 스타일
    │   └── animations.css  # 애니메이션
    └── utils/              # 유틸리티
        ├── theme-manager.js    # 테마 관리
        ├── chat-manager.js     # 채팅 관리
        ├── panel-manager.js    # 패널 관리
        └── api-client.js       # API 통신
```

## 🎨 기능

### 테마 시스템
- **6가지 색상 스킨**: Default(따뜻한 톤), Basic, Dark, Ocean, Forest, Sunset
- **5단계 글씨 크기**: XS, SM, MD(기본), LG, XL
- **유리 효과**: 투명도, 블러 조절 가능
- **배경 이미지**: 업로드 및 커스터마이징

### 레이아웃
- **Header**: 햄버거 메뉴, 알림, 사용자, 설정
- **슬라이딩 메뉴** (왼쪽): 10개 메뉴 항목, 유리 효과
- **채팅 영역**: 메시지 표시, 마크다운 지원, 입력창
- **오른쪽 패널**: 10개 패널 타입 (검색, 파일, 메모리, MCP, 아카이브 등)

### 반응형
- 데스크톱, 태블릿, 모바일 완벽 지원

## 🔧 기술 스택

- **빌드**: Vite 5
- **언어**: Vanilla JavaScript (ES6+)
- **스타일**: CSS Variables (테마 시스템)
- **마크다운**: marked.js

## 🌐 API 연동

백엔드 서버 (http://localhost:5000) 와 자동 연결
- `/api/chat` - 채팅
- `/api/profile` - 사용자 프로필
- `/api/notifications` - 알림
- `/api/panel` - 패널 관리
- 그 외 120+ 엔드포인트

## 📝 개발 가이드

### 새 패널 추가하기

`src/utils/panel-manager.js` 파일의 `panels` 객체에 추가:

```javascript
myPanel: {
  title: '📌 내 패널',
  render: () => this.renderMyPanel(),
}
```

### 테마 커스터마이징

`src/styles/themes.css` 파일에서 CSS 변수 수정:

```css
:root {
  --primary: #d4a574;
  --background: #e0ded7;
  /* ... */
}
```

### API 호출 추가

`src/utils/api-client.js`에 메서드 추가:

```javascript
async getMyData() {
  return this.get('/my-endpoint');
}
```

## 🐛 트러블슈팅

### 백엔드 연결 실패
- 백엔드 서버가 http://localhost:5000 에서 실행 중인지 확인
- `vite.config.js`의 proxy 설정 확인

### 스타일이 적용되지 않음
- 브라우저 캐시 클리어
- 개발 서버 재시작

## 📄 라이선스

MIT License
