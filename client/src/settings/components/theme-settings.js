/**
 * Theme Settings Component
 * 테마 설정 UI 컴포넌트 (플레이스홀더)
 */

export class ThemeSettings {
  async render(container, apiClient) {
    container.innerHTML = `
      <div class="settings-placeholder">
        <h3>🎨 테마 설정</h3>
        <p>테마 설정 페이지는 곧 구현됩니다.</p>
      </div>
    `;
  }
}
